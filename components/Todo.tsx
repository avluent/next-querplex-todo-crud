import React from 'react'

interface TodoProps {

}

const Todo = (props: TodoProps) => {

    return (
        <div
            className={`Todo`}
        >
            This is a to-do
        </div>
    )
}

export default Todo